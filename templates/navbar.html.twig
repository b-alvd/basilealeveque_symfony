<nav>
    <div class="nav_logo">
        <a href=" {{ path('app_index') }} ">Am-Stam-Gram</a>
    </div>
    <div class="nav_links">
        <ul>
            <li><a href=" {{ path('app_index') }} ">Accueil</a></li>
        </ul>

        {% if app.user %}
            <ul>
                <li><a href=" {{ path('app_profile_all') }}">Tous les profils</a></li>
            </ul>
            <ul>
                <li><a href=" {{ path('all_friend_requests') }} ">Demandes d'amitié</a></li>
            </ul>
            <ul>
                <li><a href=" {{ path('app_post') }} ">Créer un post</a></li>
            </ul>
            <ul>
                <li><a href=" {{ path('app_profile', { 'username': app.user.username }) }} ">Profil | {{ app.user.username }}</a></li>
            </ul>
        {% endif %}
        <ul>
            {% if app.user %}
                <li><a class="auth_button" href=" {{ path('app_logout') }} ">Se déconnecter</a></li>
            {% else %}
                <li><a class="auth_button" href=" {{ path('app_login') }} ">Se connecter</a></li>
            {% endif %}
        </ul>
    </div>
</nav>
